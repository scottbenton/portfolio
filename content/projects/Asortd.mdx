**A**lgorithm **SORT**ing **D**isplay.
On this project, I implemented the ability to let users define custom sorting algorithms.
I also refactored the code to add the ability to step through the animation rather than just running it straight through.
